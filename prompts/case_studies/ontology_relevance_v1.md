# Ontology relevance prompt — version 1

Explain in at most 100 words why the supplied literary evidence illuminates the frozen P/T/E/O
scores. Explain; do not rescore, correct, or replace the annotation. State explicitly that the
scores are frozen. Return JSON with `status` and `note`.
